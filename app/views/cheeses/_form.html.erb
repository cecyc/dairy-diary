<%= form_for(@cheese) do |f| %>
  <% if @cheese.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cheese.errors.count, "error") %> prohibited this cheese from being saved:</h2>

      <ul>
      <% @cheese.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :thoughts %><br>
    <%= f.text_area :thoughts %>
  </div>
  <div class="field">
    <%= f.label :category_id %><br>
    <%= f.collection_select :category_id, Category.all, :id, :name, class: "collection" %>
  </div>
  <div class="image-upload">
    <%= f.attachment_field :cheese_image %>
  </div>
  <div class="form-footer">
    <%= button_to "Submit", @cheese, class: "btn-primary" %>
  </div>
<% end %>
